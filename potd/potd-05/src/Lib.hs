module Lib
    ( split
    , merge
    , mergesort
    ) where

-- Your code here!

split :: Ord a => [a] -> ([a],[a])
split = undefined

merge :: Ord a => [a] -> [a] -> [a]
merge = undefined

mergesort :: Ord a => [a] -> [a]
mergesort = undefined
